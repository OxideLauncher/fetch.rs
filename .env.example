# Oxide Meta - Environment Configuration
# Copy this file to .env or .env.local and fill in the values

# Logging level
RUST_LOG=warn,oxide_meta=trace

# Base URL where metadata will be served (your public URL)
BASE_URL=https://meta.oxidelauncher.org

# Concurrency limit for parallel requests
CONCURRENCY_LIMIT=10

# ===============================
# Cloudflare R2 Configuration
# ===============================
# Your R2 account ID (found in Cloudflare dashboard under R2 > Overview)
S3_URL=your_cloudflare_account_id

# Set to "r2" for Cloudflare R2
S3_REGION=r2

# Your R2 bucket name
S3_BUCKET_NAME=oxide-meta

# R2 API Token (create at Cloudflare Dashboard > R2 > Manage R2 API Tokens)
# Needs "Object Read & Write" permission for the bucket
S3_ACCESS_TOKEN=your_r2_access_key_id
S3_SECRET=your_r2_secret_access_key

# ===============================
# Cloudflare Cache Purging (Optional but Recommended)
# ===============================
# Set to true to enable automatic cache purging after uploads
CLOUDFLARE_INTEGRATION=true

# API Token with "Zone.Cache Purge" permission
# Create at: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_TOKEN=your_cloudflare_api_token

# Zone ID for your domain (found in domain overview page)
CLOUDFLARE_ZONE_ID=your_cloudflare_zone_id
